name: job test
on: push
jobs:
  web:
    name: deploy web
    runs-on: ubuntu-22.04
    defaults:
      run: 
        working-directory: ./web
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: echo
        run: |
          pwd
          npm --version
          node --version
        
  
